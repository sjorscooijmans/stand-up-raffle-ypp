import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'stand-up-raffle-ypp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
